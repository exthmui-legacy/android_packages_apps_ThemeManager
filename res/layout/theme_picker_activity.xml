<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ThemePickerActivity" >

    <GridView
        android:id="@+id/themesGrid"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:cacheColorHint="@android:color/transparent"
        android:clipToPadding="false"
        android:drawSelectorOnTop="false"
        android:horizontalSpacing="@dimen/picker_grid_horizontal_spacing"
        android:paddingStart="@dimen/picker_grid_padding_start"
        android:paddingTop="@dimen/picker_grid_padding_top"
        android:paddingEnd="@dimen/picker_grid_padding_end"
        android:paddingBottom="@dimen/picker_grid_padding_bottom"
        android:scrollbarAlwaysDrawVerticalTrack="true"
        android:scrollbarStyle="outsideOverlay"
        android:stretchMode="columnWidth"
        android:verticalSpacing="@dimen/picker_grid_vertical_spacing" />
</FrameLayout>
